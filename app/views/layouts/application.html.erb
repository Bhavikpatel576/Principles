<!DOCTYPE html>
<html>
  <head>
    <title>Our Principles</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://getbootstrap.com/docs/4.0/examples/cover/cover.css" rel="stylesheet">
  </head>

  <body>
          <nav class="navbar navbar-default">
            <div class="container-fluid">
              <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
              <a class="navbar-brand" href="#">Our Principles</a>
              <ul class="nav navbar-nav">
                <li class="nav-item">
                  <a class="nav-link" href="/">Home</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="#About">About</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link disabled" href="#Contact">Contact</a>
                </li>
                <li class="nav-item">
                  <% if signed_in? %>
                    <%= button_to 'Sign out', sign_out_path, method: :delete, :form_class=>"nav-link disable" %>
                    <span class="navbar-text"> Signed in as: <%= current_user.email %> </span> 
                  <% else %>
                    <%= link_to 'Sign in', sign_in_path, { :class=>"nav-link" } %>
                  <% end %>
                </li>
              </ul>
              <%= render "search" %>
          </nav>
        </div>
      </header>
   
  <div id="flash">
    <% flash.each do |key, value| %>
      <div class="flash <%= key %>"><%= value %></div>
    <% end %>
  </div>
    
    <%= yield %>
  </body>
</html>
